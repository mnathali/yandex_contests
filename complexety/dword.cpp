#include <iostream>

int main(int argc, char const *argv[])
{
    int w, h, c, x{}, y{};
    std::string s;
    std::cin >> w >> h >> c;
    while (std::getline(std::cin, s))
    {
        std::cout << s << std::endl;
        
    }
    return 0;
}
